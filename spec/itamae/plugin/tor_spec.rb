# frozen_string_literal: true

RSpec.describe Itamae::Plugin::Tor do
  it "has a version number" do
    expect(Itamae::Plugin::Tor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
